RewriteEngine On
RewriteBase /bug-tracking-system/

# If the request is not for a file or directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite to index.php
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]